"""MediaTracker — track, snapshot and reproduce online-journal articles and
their comment threads over time, for private local media research.

Sibling in spirit to the algotrade component set: an asyncio websocket daemon,
raw-SQL Postgres with a self-migrating schema, JSONL graceful-degradation store,
stdlib-first fetching, and a content-addressed blob lake for images.
"""
from __future__ import annotations

__version__ = "0.1.0"
